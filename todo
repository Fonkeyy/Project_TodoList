add toggle display project list

add possibility to change "due" on todoCard
add labels on every buttons + hide it

add hide label class
refactor all inputs

change font-size 14px
add flag svg on priority select

add modify button on todoCard

add padding to header buttons
add local storage

check accessibility
check performance

add little squeeze effect when click on priority or project select
