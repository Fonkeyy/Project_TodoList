add toggle display project list on sidebar

refactor all inputs

add padding to header buttons
add local storage

add labels on every buttons + hide it
add hide label class
check accessibility
check performance

add little squeeze effect when click on priority or project select

style todoCard
change description placeholder color

add remove btn on duedate todoCard

remove todo when checkbox clicked + set it to archive folder

add functionality to 3 dots button on todo:
-add modify button on todoCard
-add remove btn

fix recurrent errors
